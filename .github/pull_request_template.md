## Summary of Changes
- Briefly describe the editorial or architecture change.

## Canonical Skill Checklist
- [ ] Edited `skills/blog-writer/SKILL.md` (single source of truth).
- [ ] Synchronized `.cursor/skills/blog-writer/SKILL.md` via `python scripts/sync_skills.py`.
- [ ] Ran all unit tests (`python -m unittest discover -s tests -v`).
- [ ] Tested against `evals/cases.jsonl` without regressions in Differentiation Score.
